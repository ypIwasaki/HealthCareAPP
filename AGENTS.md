# Repository instructions

## Agent skills

### Issue tracker

Issue と仕様書は `.scratch/<feature-slug>/` 配下のローカル Markdown で管理する。Issue の作成・取得・更新時は `docs/agents/issue-tracker.md` を読む。

### Triage labels

既定の5ラベルを使用する。Issue の分類・状態更新時は `docs/agents/triage-labels.md` を読む。

### Domain docs

single-context 構成とし、用語集はルートの `CONTEXT.md`、設計判断は `docs/adr/` に置く。コードベースの調査前に `docs/agents/domain.md` を読む。
