# 計算に使ったプロフィールの値を記録ごとに保持する

身長を初期設定で登録して BMI を自動計算するが、各体重記録には実際に計算に使った身長を対応付けて保持する。常に最新のプロフィールで過去の記録を再計算する方式は採用せず、身長の設定を変更しても過去の BMI やグラフが勝手に変わらないことを優先する。これは BMI の計算用身長に関する決定である。体脂肪量・骨格筋量の換算式を変更した場合は、[ADR-0007](0007-recalculate-history-with-saved-formulas.md) に従って過去分も再計算する。
