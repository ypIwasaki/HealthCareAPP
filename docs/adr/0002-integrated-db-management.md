# 認証なしの DB 管理画面をアプリ内に設ける

DB の内容を GUI で直接確認・操作したいという要望に対し、外部の DB 管理ツールだけに任せず、アプリ内に DB 管理画面を設ける。画面の実装・保守は増えるが、一つのアプリ内でデータ操作まで行えることを優先する。開発者本人が個人用の Windows PC 内で使う初版では管理画面への認証を設けず、初版の操作はアプリが使用するデータ行の閲覧・追加・修正・削除に限定する。

SQL の自由入力やテーブル・列の追加は初版の範囲外とし、通常画面と管理画面で共通の入力ルールを適用する。
