# 保存した換算式を過去分を含む全記録に適用する

体脂肪量・骨格筋量の2つの換算式をアプリ内で編集し、DB に保存する。式を変更したら過去分も同じ式で再計算するという要望を優先し、記録ごとの古い式で換算値を固定する方式は採用しない。入力した体重と率を正本として残し、換算値は現在有効な式から求めることで、元の記録を保ったまま一覧・グラフへ一貫して反映する。

バックアップには記録と有効な式を一緒に含める。BMI の標準式と記録ごとの計算用身長はこの換算式の変更対象に含めず、[ADR-0006](0006-preserve-calculation-inputs.md) の方針を維持する。
